extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="scrape", action="/scrape", method="post")
      input(type="text", name="url")
      input(type="submit", value="Clean Up")
